# Leetcode162
Find peak element in array

The solution is actually very simple. I'm still trying to solve this in a better way. Here is how it works:
      + Firstly, find the peak element in the array.
      + Then traverse through the array to find the element that equals to the peak value, then return the index of that element.
